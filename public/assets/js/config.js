var primary = localStorage.getItem("primary") || '#00b9f2';
var secondary = localStorage.getItem("secondary") || '#a6ce39';

window.CubaAdminConfig = {
	// Theme Primary Color
	primary: primary,
	// theme secondary color
	secondary: secondary,
};
